export enum MANIFEST {
  manifest_loadable = "manifest-loadable.json",
  manifest_deps = "manifest-deps.json",
  manifest_dev = "manifest-dev.json",
  manifest_prod = "manifest-prod.json",
  manifest_static = "manifest-static.json",
}
